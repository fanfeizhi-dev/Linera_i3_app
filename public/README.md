# Firebase 集成说明

## 文件结构

```
public/
├── firebase-config.js      # Firebase 配置文件
├── index.html              # 主测试页面
├── test-firebase.html      # 简单测试页面
└── README.md               # 说明文档
```

## 使用方法

### 1. 启动服务器
```bash
npm start
```

### 2. 访问测试页面
- 主页面：`http://localhost:3000/public/`
- 测试页面：`http://localhost:3000/public/test-firebase.html`

### 3. 检查控制台
打开浏览器开发者工具，查看控制台输出：
- 如果看到 "🔥 Firebase 初始化成功！" 说明配置正确
- 如果看到错误信息，请检查配置

## 配置说明

### Firebase 配置
- **项目ID**: i3-testnet
- **域名**: i3-testnet.firebaseapp.com
- **API Key**: 已配置在 firebase-config.js 中

### 当前启用的服务
- ✅ Firebase App (核心应用)
- ✅ Firebase Analytics (分析服务)

## 添加更多 Firebase 服务

如果需要使用其他 Firebase 服务，可以在 `firebase-config.js` 中添加：

```javascript
// 导入其他服务
import { getFirestore } from "firebase/firestore";
import { getAuth } from "firebase/auth";
import { getStorage } from "firebase/storage";

// 初始化服务
const db = getFirestore(app);
const auth = getAuth(app);
const storage = getStorage(app);

// 导出
export { app, analytics, db, auth, storage };
```

## 注意事项

1. **模块类型**: 使用 ES6 模块 (`type="module"`)
2. **浏览器兼容**: 需要支持 ES6 模块的现代浏览器
3. **CORS**: 确保服务器正确配置了 CORS 策略
4. **环境变量**: 生产环境建议使用环境变量存储敏感配置

## 故障排除

### 常见问题
1. **模块加载失败**: 检查文件路径和模块语法
2. **CORS 错误**: 检查服务器 CORS 配置
3. **配置错误**: 验证 Firebase 项目配置信息

### 调试步骤
1. 检查浏览器控制台错误信息
2. 验证网络请求是否成功
3. 确认 Firebase 项目状态
4. 检查依赖是否正确安装

## 下一步

Firebase 配置完成后，您可以：
1. 在现有页面中导入并使用 Firebase 服务
2. 添加用户认证功能
3. 集成 Firestore 数据库
4. 使用 Firebase Storage 存储文件
5. 添加实时数据同步功能 